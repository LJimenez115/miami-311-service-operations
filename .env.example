# Why: Copy this file to `.env` and replace only the password; Git ignores `.env` so credentials stay local.
DATABASE_URL=postgresql+psycopg://postgres:replace_with_your_password@localhost:5432/miami_311_operations
